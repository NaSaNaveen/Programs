#include<stdio.h>
union Scope
{
    struct st
    {
        char alpha;
        int num;
    };
};

int main()
{
    union Scope x;
    scanf("%d %c",&x.num,&x.alpha);
    printf("%c  %d", x.alpha, x.num);
    return 0;
}
